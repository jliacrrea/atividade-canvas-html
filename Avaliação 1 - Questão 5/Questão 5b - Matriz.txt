<!DOCTYPE html>
<html>

<head>
  
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width">
  <title>Matriz</title>
  
</head>

<body>
  
    <canvas width=610 height=610></canvas>
 
    <script>
      let canvas = document.querySelector("canvas");
      let ctx = canvas.getContext("2d");

      ctx.strokeStyle="#2F528F";
      ctx.lineWidth = 3;
      
      for(let i=0; i<5; i++){
        for(let j=0; j<5; j++){

          // Quadrado
          ctx.strokeRect(20 + (120 * i), 20 + (120 * j), 100, 100);
          
          // Triângulo
          ctx.beginPath();
          ctx.moveTo(110 + (120 * i), 95 + (120 * j));
          ctx.lineTo(30 + (120 * i), 95 + (120 * j));
          ctx.lineTo(70 + (120 * i), 40 + (120 * j));
          ctx.closePath();
          ctx.stroke();
        }
      }
      
    </script>

</body>

</html>
